# Default values for drift-report-plugin.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

global:
  persistence:
    url: "" # Endpoint for the object storage. Compatible with S3 or Minio.
    service:
      ports:
        api: 9000

  manager:
    service:
      ports:
        http: 8080
        grpc: 8081
  tolerations: []

image:
  full: hydrosphere/drift-report-plugin:d14cb4ee0a709b3d85d1eee4f5518e4177b065b1
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000

podSecurityContext:
  runAsUser: 42069

securityContext:
  allowPrivilegeEscalation: false

podAnnotations: {}

resources: {}

nodeSelector: {}

affinity: {}
